{% extends 'MaithPumpMyGasBundle::layout.html.twig' %}

{% block body -%}
<div class="row">
  <div class="col-lg-12">
    <section class="panel">
      <header class="panel-heading">{% trans %}brands_update_entry{% endtrans %}</header>
      <div class="panel-body">
        <form action="{{ path('admin_pump_brand_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }} role="form">
		  <input type="hidden" name="_method" value="PUT" />  
          <div class="form-group">
			  {{ form_errors(edit_form.name) }}
              <label for="{{ edit_form.name.vars.id }}">{% trans %}brands_name{% endtrans %}</label>
			  {{ form_widget(edit_form.name, {'attr' : {'class' : 'form-control'}}) }}
          </div>
          <div class="form-group">
            <button type="submit" class="btn btn-default">{% trans %}brands_save{% endtrans %}</button>
          </div>	
              {{ form_rest(edit_form) }}
          </form>

        </div>
      </section>
    </div>
    <div class="col-lg-8">
        <a href="{{ path('admin_pump_brand') }}" class="btn btn-info">
		  {% trans %}brands_back_to_list{% endtrans %}
	  </a>
		<form action="{{ path('admin_pump_brand_delete', { 'id': entity.id }) }}" method="post" onsubmit="return confirmDeleteDialog();" style="float: right;">
		  <input type="hidden" name="_method" value="DELETE" />
			{{ form_widget(delete_form) }}
		  <button class="btn btn-danger" type="submit">{% trans %}brands_delete{% endtrans %}</button>
		</form>
	</div>	
</div>
{% endblock %}
