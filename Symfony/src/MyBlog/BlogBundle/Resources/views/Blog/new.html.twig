{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Blog creation</h1>

    <form action="{{ path('my_blog_crud_blog_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('my_blog_crud_blog_list') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
