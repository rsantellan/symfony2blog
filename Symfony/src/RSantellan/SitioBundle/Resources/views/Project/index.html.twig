{% extends 'RSantellanSitioBundle::layout.html.twig' %}

{% block body -%}
    <h1>Project list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Cliente</th>
                <th>Tipo_de_trabajo</th>
                <th>Description</th>
                <th>Orden</th>
                <th>Slug</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('rsproject_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.cliente }}</td>
                <td>{{ entity.tipodetrabajo }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.orden }}</td>
                <td>{{ entity.slug }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('rsproject_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('rsproject_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('rsproject_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
